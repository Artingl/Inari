{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Kernel Debug",
            "type": "gdb",
            "request": "attach",
            // "executable": "./binutils/mouse.exe",
            "executable": "./build/kernel.elf",
            "target": ":1234",
            "remote": true,
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText",
            // "preLaunchTask": "build-and-run",
            // "postDebugTask": "kill-qemu"
        }
    ]
}
